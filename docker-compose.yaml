version: '3.8'

services:
  lokalny-keycloak:
    image: quay.io/keycloak/keycloak:latest
    container_name: lokalny-keycloak
    ports:
      - "8080:8080"
    environment:
      - KEYCLOAK_ADMIN=admin
      - KEYCLOAK_ADMIN_PASSWORD=admin
    command: start-dev
